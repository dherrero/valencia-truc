# shared-game-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-game-engine` to build the library.

## Running unit tests

Run `nx test shared-game-engine` to execute the unit tests via [Jest](https://jestjs.io).
